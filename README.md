# SATYX Multi-Page Store

Pages: Home, Shop, New Arrivals, Drop 001, Product, About, Account, Cart, Checkout.

All navigation uses separate HTML pages. Cart/account data uses browser localStorage for prototype use.

Before launch: replace mock product visuals with real photos, add secure backend authentication/database, connect Razorpay/Stripe, and add real policies.

Edit products inside `script.js` under `const PRODUCTS=[...]`.
